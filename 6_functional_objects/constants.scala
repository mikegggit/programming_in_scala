/*
constants in scala should be named using camel-case.

a constant in scala is more than simply a val.

method parms may be vals, but the parm is not constant...
...that is, different values may be passed into the method.

Constants should only refer to things that never change.
*/

